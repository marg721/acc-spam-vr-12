
const Discord = require("discord.js"); ////////////////////// bas bro id voice da bne
const prefix = [""];//////////// prefix lera dane 
const jano = new Discord.Client();
const jano2 = new Discord.Client();
const jano3 = new Discord.Client();
const jano4 = new Discord.Client();
const jano5 = new Discord.Client();
const jano6 = new Discord.Client();
const jano7 = new Discord.Client();
const jano8 = new Discord.Client();
const jano9 = new Discord.Client();
const jano10 = new Discord.Client();
///////////////////////////////
jano.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});

///////////////////////////////
jano2.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});

///////////////////////////////
jano3.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});

///////////////////////////////
jano4.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});

///////////////////////////////
jano5.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});

///////////////////////////////
jano6.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});

///////////////////////////////
jano7.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});

///////////////////////////////
jano8.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});

///////////////////////////////
jano9.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});

///////////////////////////////
jano10.on("message", message => {
  if (message.content === prefix + "1k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 1000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "2k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 2000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "3k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 3000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "f4k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 4000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "5k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 5000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "6k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 6000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "7k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 7000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "8k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 8000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 9000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
  if (message.content === prefix + "10k") {
    let count = 0;
    let ecount = 0;
    for (let x = 0; x < 10000; x++) {
      message.channel.send(`owo daily`).then(m => {
        count++;
      });
    }
  }
});
////////////////////////////
//////////////////////////
jano.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
//////////////////////////
jano2.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
//////////////////////////
jano3.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
//////////////////////////
jano4.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
//////////////////////////
jano5.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
//////////////////////////
jano6.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
//////////////////////////
jano7.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
//////////////////////////
jano8.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
//////////////////////////
jano9.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
//////////////////////////
jano10.on("message", message => {
  if (message.content === prefix + "hello") {
    let letters = "hcfgbeqlgfcequkgrhqwertyuiop[]';lkjhgfdsazxcvbnm,./1234567890'".split(
      ""
    );
    let spaces = " ".repeat(10);
    let last = "";
    let messages = 0;

    setInterval(() => {
      let str = "";
      let num = Math.floor(Math.random() * 10);
      for (let i = 0; i < num; i++) {
        str += letters[Math.floor(Math.random() * letters.length)];

        if (i === num - 1) {
          if (last === "") {
            messages += 1;
            last = `${str} ${spaces.slice(str.length + 1)}`;
            return message.channel.send(
              `${str} ${spaces.slice(str.length + 1)}${messages}`
            );
          } else {
            messages += 1;
            let spaced = `${str} ${spaces.slice(str.length + 1)}${messages}`;
            last = spaced;
            return message.channel.send(spaced);
          }
        }
      }
    }, 6000);
  }
});
////////////////////////////////////////////// bas id voice dane 
jano.on("ready", () => {
    var join = jano.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////
jano2.on("ready", () => {
    var join = jano2.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////
jano3.on("ready", () => {
    var join = jano3.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////
jano4.on("ready", () => {
    var join = jano4.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////
jano5.on("ready", () => {
    var join = jano5.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////
jano6.on("ready", () => {
    var join = jano6.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////
jano7.on("ready", () => {
    var join = jano7.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////
jano8.on("ready", () => {
    var join = jano8.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////
jano9.on("ready", () => {
    var join = jano9.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////
jano10.on("ready", () => {
    var join = jano10.channels.get(""); /// id voice
    if (join) join.join();
  });
///////////////////

//////// playing or dnd  streaming ////////

jano.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano.user.setActivity(STREAMING, {      
    type: "STREAMING", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

jano2.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano2.user.setActivity(STREAMING, {      
    type: "playing", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

jano3.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano3.user.setActivity(STREAMING, {      
    type: "playing", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

jano4.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano4.user.setActivity(STREAMING, {      
    type: "playing", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

jano5.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano5.user.setActivity(STREAMING, {      
    type: "playing", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

jano6.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano6.user.setActivity(STREAMING, {      
    type: "playing", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

jano7.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano7.user.setActivity(STREAMING, {      
    type: "playing", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

jano8.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano8.user.setActivity(STREAMING, {      
    type: "playing", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

jano9.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano9.user.setActivity(STREAMING, {      
    type: "playing", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

jano10.on("ready", () => {
  console.log(
    `Online In Servers`
  );
  let statuses = [
  
  `I rely on God that's why no one can destroy him`,

  ];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    jano10.user.setActivity(STREAMING, {      
    type: "playing", 
    url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////



//////////////// tokn lera da bne 

jano.login("NzgwNDI5OTQwODM0OTU5Mzgx.X7u-kQ.XMn2lxgJI_h4GECpV9E6YXOYhP4");
jano2.login("");
jano3.login("");
jano4.login("");
jano5.login("");
jano6.login("");
jano7.login("");
jano8.login("");
jano9.login("");
jano10.login("");

